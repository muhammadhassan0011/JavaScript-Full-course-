"use strict";
const firstName = " Hassan";
// console.log(months);  // not secess to months

//   // if activity running, create running object ___
//     if (type === "running") {
//       const cadence = +inputCadence.value;
//       // Check if data is valid : using Guard clause :==> checking for opposite of what we are intrested in and if that opposite is true , then simply return func.. immediately.
//       if (
//         // !Number.isFinite(distance) ||
//         // !Number.isFinite(duration) ||
//         // !Number.isFinite(cadence)
//         !validInputs(distance, duration, cadence) ||
//         !allPositive(distance, duration, cadence)
//       )
//         return alert("input have to be posite");
//       // if distance is not Number then return ...
//     }
//     // if activity cycling , create cycling object ___
//     if (type === "cycling") {
//       const elevation = +inputElevation.value;

//       if (
//         !validInputs(distance, duration, elevation) ||
//         !allPositive(distance, duration)
//       )
//         return alert("input have to be posite");
//       // Check if data is valid :
//     }
